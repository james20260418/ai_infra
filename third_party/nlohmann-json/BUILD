# nlohmann/json 单头（header-only，无依赖）
#
# v3.11.3，来自官方 single_include 包。仅供 gen3d parser 稳健解析
# tripo3d 的嵌套 JSON 响应（code/data/status/output.model_url）。
# 不链接任何库，仅头文件。

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "json",
    hdrs = ["include/nlohmann/json.hpp"],
    includes = ["include"],
)

package(default_visibility = ["//visibility:public"])

# Windows x86_64 target platform for MinGW cross-compilation
platform(
    name = "windows_x86_64",
    constraint_values = [
        "@platforms//os:windows",
        "@platforms//cpu:x86_64",
    ],
)
